# ─────────────────────────────────────────────
# Drive-IT — Python Microservice Environment Variables
# Copy this to python-microservice/.env
# ─────────────────────────────────────────────

# Only needed here if you later add Groq-backed logic on the Python side.
# Currently this service only runs Guardrails + LangGraph output checks,
# which don't call any LLM API themselves.
GROQ_API_KEY=your_groq_api_key_here
